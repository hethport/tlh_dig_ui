query Index {
    allManuscripts {
        mainIdentifier {
            type
            identifier
        }
    }
}

mutation Register($userInput: UserInput!) {
    register(userInput: $userInput)
}
